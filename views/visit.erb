<h2>Make an appointment</h2>
<p>Here you can make an appointment</p>

<form action="/visit" method="POST" role="form">

  <div class="form-group">
    <label>Your name</label>
    <input name="client[name]" type="text" class="form-control" placeholder="Your name" value="<%= @c.name %>">
  </div>

  <div class="form-group">
    <label>Your phone</label>
    <input name="client[phone]" type="text" class="form-control" placeholder="Your phone" value="<%= @c.phone %>">
  </div>

  <div class="form-group">
    <label>Date and time?</label>
    <input id="datetimepicker" name="client[datestamp]" autocomplete="off" type="text" class="form-control" placeholder="Date and time" value="<%= @c.datestamp %>">
  </div>

  <div class="form-group">
    <label>Your barber:</label>    
    <select class="form-control" name="client[barber]">

      <% @barbers.each do |item| %>
         <option <%= @c.barber == item['name'] ? 'selected' : '' %>>
          <%= item['name'] %></option>
      <% end %>

    </select>
  </div>

  <div class="form-group">
    <label>What color do you like?</label>
    <select id="color"  name="client[color]">
      <option value="#7bd148">Green</option>
      <option value="#fbd75b">Yellow</option>
      <option value="#ff887c">Red</option>
      <option value="#e1e1e1">Grey</option>
    </select>
  </div>

  <button type="submit" class="btn btn-primary">Submit</button>

</form>

<script type="text/javascript">
$(function() {
  $('#color').simplecolorpicker();
  $('#datetimepicker').datetimepicker();
})
</script>